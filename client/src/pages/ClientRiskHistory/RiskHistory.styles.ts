import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    container: {
        margin: 10,
        padding: 10,
        border: "1px solid #aaa",
        borderRadius: 5,
        fontSize: "120%",
    }
})