import { StyleSheet } from "react-native";

const useStyles = () =>
    StyleSheet.create({
        headerStyle: {
            backgroundColor: "#273263",
        },
    });

export default useStyles;
