import { SxProps, Theme } from "@mui/material";

export const riskLevelChipStyles: Record<string, SxProps<Theme>> = {
    chip: {
        "& .MuiChip-label": {
            color: "white",
        },
    },
};
