import { SxProps, Theme } from "@mui/material";

export const alertListStyles: Record<string, SxProps<Theme>> = {
    alertSubject: {
        display: "inline",
        paddingRight: 1,
        fontSize: 18,
    },
    alertInfoText: {
        display: "inline",
        fontSize: 14,
    },
};
