import { SxProps, Theme } from "@mui/material";

export const adminStyles: Record<string, SxProps<Theme>> = {
    fieldIndent: {
        paddingLeft: "9px",
    },
    hipWidth: {
        maxWidth: "160px",
    },
    inches: {
        verticalAlign: "sub",
    },
};
